# Bandit Level 7 -> Level 8

## Level Goal
The password for the next level is stored in the file data.txt next to the word millionth.

## Key concepts learnt
1. `grep` command is used to search inside files for specific patterns.
2. `Piping` is a method to direct the output of one command to serve directly as the input to another command.
